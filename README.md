# Nudzie

A friendly pixel character walks in from a corner of your screen to nudge you —
**before calendar meetings** and on **your own interval schedule** (water,
breaks, whatever). Accept or snooze; the character celebrates, then walks off.

Nudzie is an Electron desktop app (TypeScript + Vite via `electron-vite`,
packaged with `electron-builder`).

## Run it

```bash
npm install
npm run dev
```

You'll get:

- a **menu-bar / tray icon** with manual controls (Remind me now, Trigger a test
  reminder, Pause reminders, Quit),
- a **settings window** to configure both reminder types,
- **calendar-triggered reminders** (Google Calendar + iCloud CalDAV + iCal/.ics
  links), and
- **interval-triggered reminders** (active hours + interval + snooze),

…all routed through the same **corner-walk character overlay** with accept/snooze.

Press **⌘⇧D** (or use the tray) to trigger a test reminder immediately.

## Windows Store (MSIX) build

`npm run build:windows` produces the unsigned Microsoft Store package at
`dist/Nudzie-<version>.appx` (`.appx` and `.msix` are the same container format;
Partner Center accepts the `.appx` upload).

**It must run on a real Windows machine or VM** — the packaging step shells out
to `makeappx.exe`. On macOS it fails with `spawn prlctl ENOENT`, which is
electron-builder looking for a Parallels Windows VM. In CI the `msix` job in
`.github/workflows/release.yml` builds it on `windows-latest` and uploads it as
the `nudzie-msix` workflow artifact.

Nothing here is signed and nothing is published: Microsoft re-signs the package
server-side on submission, so the build intentionally stops at the `.appx`, which
you then upload to Partner Center by hand. Identity values live in the `appx:`
block of `electron-builder.yml` and must match the reserved Partner Center
identity exactly.

Store tile artwork is generated from the app icon by `npm run gen:appx-assets`
into `build/appx/` (gitignored, like the other generated icons). That folder name
is fixed by electron-builder — it reads user tile assets only from
`<buildResources>/appx`, and silently substitutes its own sample artwork for any
logo that's missing.

## Character art

The default "Buddy" sprites are placeholders, and Buddy's face is also the
menu-bar / tray icon (generated by the asset pipeline into `build/tray.png`).

**Two ways to use your own character:**

**A) In-app (no terminal) — "Make your own", in Settings → Character.**
Upload an *idle* pose and an *action* pose; Nudzie removes the background, trims,
and starts using your character (and its face becomes the tray icon) — all
processed **on your device**, nothing uploaded. Settings shows a copy-paste AI
prompt recipe for generating a matching pair. This is a per-user custom
character; it is a **Pro-gated** capability. Use `NUDZIE_FORCE_PRO=1` for local
testing (see [PRO-TIER.md](PRO-TIER.md), `CUSTOM_CHARACTER_REQUIRES_PRO`).

**B) Build-time (ships a new built-in character):**

1. Drop `idle.png` and `action.png` into `assets/raw/` (or
   `assets/raw/<character-id>/`).
2. Run `npm run prepare-assets` (or `npm run prepare-assets -- <character-id>`).
   This strips the flat background, feathers the edges, trims margins, writes
   sprites into `src/renderer/overlay/characters/<id>/`, and (for the default
   character) refreshes `build/tray.png`.
3. Register the character in `src/renderer/characters.ts`.

The **app icon (Dock / taskbar) is also the character's face** — generated onto a
rounded tile by `prepare-assets` (`build/icon-master.png` → `build/icon.png`). When
a user sets a custom character, the app icon updates to their character live
(macOS Dock via `app.dock.setIcon`, Windows/Linux via the window icon). To force a
completely different app icon instead, drop your own `build/icon-master.png`
(1024×1024) — it takes priority; icons regenerate on `npm run gen:icons` (run
automatically by `dev`/`build`).

## Google Calendar setup (optional for this baseline)

Calendar reminders for Google need OAuth credentials. Copy
`oauth-credentials.example.json` to `oauth-credentials.json` and fill in your own
`clientId` / `clientSecret` (or paste them in-app / set `GOOGLE_CLIENT_ID` +
`GOOGLE_CLIENT_SECRET`). Without this, Google simply stays disconnected — the app
still builds and runs, and iCloud / iCal links work independently.

## Pro tier

Nudzie keeps an open-core license-check scaffold (free vs Pro gating) so extra
characters, sounds, and themes can be sold without changing the app's core
architecture. Pro is backed by Dodo Payments license-key entitlements, with a
14-day offline grace window and one-device activation. See
[PRO-TIER.md](PRO-TIER.md).

## Attribution

Nudzie is built on two MIT-licensed open-source projects:

- **[Quakpit](https://github.com/Ooble-Studio/QuakPit)** by **Ooble Studio** —
  the architectural base for the **calendar integration, scheduler, windowing,
  tray, persistent store, and open-core license-check system**.
- **Hydrate Buddy** by **sharada-dev** — the architectural base for the
  **corner-walk character animation system** (walk-in/walk-off, sprite swap,
  accept/snooze/celebration) and the **jimp-based `prepare-assets` art pipeline**.

Both projects' MIT terms are preserved in [LICENSE](LICENSE). See
[ARCHITECTURE.md](ARCHITECTURE.md) for exactly what was ported from each.
